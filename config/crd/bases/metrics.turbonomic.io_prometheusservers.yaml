---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: prometheusservers.metrics.turbonomic.io
spec:
  group: metrics.turbonomic.io
  names:
    kind: PrometheusServer
    listKind: PrometheusServerList
    plural: prometheusservers
    singular: prometheusserver
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PrometheusServer is the Schema for the prometheusservers API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: PrometheusServerSpec defines the desired state of PrometheusServer
            properties:
              address:
                description: Address of the Prometheus server.
                type: string
              clusters:
                description: Information about the clusters which this Prometheus
                  server should obtain metrics for. If not specified, or empty, it
                  is assumed that all metrics obtained are for the cluster where the
                  Prometurbo probe is running.
                items:
                  properties:
                    clusterLabels:
                      additionalProperties:
                        type: string
                      description: The labels that identify the cluster when executing
                        PromQL query against the Prometheus server
                      type: object
                    exporterSelector:
                      description: The Label selector for PrometheusExporters. If
                        not defined, defaults to all PrometheusExporter resources
                        in the current namespace.
                      properties:
                        matchExpressions:
                          description: matchExpressions is a list of label selector
                            requirements. The requirements are ANDed.
                          items:
                            description: A label selector requirement is a selector
                              that contains values, a key, and an operator that relates
                              the key and values.
                            properties:
                              key:
                                description: key is the label key that the selector
                                  applies to.
                                type: string
                              operator:
                                description: operator represents a key's relationship
                                  to a set of values. Valid operators are In, NotIn,
                                  Exists and DoesNotExist.
                                type: string
                              values:
                                description: values is an array of string values.
                                  If the operator is In or NotIn, the values array
                                  must be non-empty. If the operator is Exists or
                                  DoesNotExist, the values array must be empty. This
                                  array is replaced during a strategic merge patch.
                                items:
                                  type: string
                                type: array
                            required:
                            - key
                            - operator
                            type: object
                          type: array
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: matchLabels is a map of {key,value} pairs.
                            A single {key,value} in the matchLabels map is equivalent
                            to an element of matchExpressions, whose key field is
                            "key", the operator is "In", and the values array contains
                            only "value". The requirements are ANDed.
                          type: object
                      type: object
                    id:
                      description: 'The unique ID of the cluster. Get the ID by running
                        the following command in the cluster: kubectl -n default get
                        svc kubernetes -ojsonpath=''{.metadata.uid}'''
                      type: string
                  type: object
                type: array
              password:
                description: Password of the Prometheus server.
                type: string
              username:
                description: Username of the Prometheus server.
                type: string
            required:
            - address
            type: object
          status:
            description: PrometheusServerStatus defines the observed state of PrometheusServer
            properties:
              clusters:
                items:
                  properties:
                    entities:
                      items:
                        properties:
                          count:
                            format: int32
                            type: integer
                          type:
                            type: string
                        required:
                        - count
                        - type
                        type: object
                      type: array
                    id:
                      type: string
                    lastDiscoveryTime:
                      format: date-time
                      type: string
                  required:
                  - id
                  type: object
                type: array
              message:
                type: string
              reason:
                type: string
              state:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
